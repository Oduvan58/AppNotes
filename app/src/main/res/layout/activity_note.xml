<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.NoteActivity">

    <TextView
        android:id="@+id/title_note_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/title_note_margin_start"
        android:paddingBottom="@dimen/title_note_padding_bottom"
        android:textColor="@color/black"
        android:textSize="@dimen/title_note_text_size"
        android:textStyle="italic"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Название" />

    <TextView
        android:id="@+id/description_note_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_color_description_date"
        android:textSize="@dimen/description_note_text_size"
        app:layout_constraintStart_toStartOf="@id/title_note_text_view"
        app:layout_constraintTop_toBottomOf="@id/title_note_text_view"
        tools:text="Описание" />

</androidx.constraintlayout.widget.ConstraintLayout>